# Feature A — Members + Plans + Subscriptions (Owner 1) · Progress

> Part of the gym progress log. Overview/index: [`../PROGRESS.md`](../PROGRESS.md) ·
> Plan: [`../gym-management-plan.md`](../gym-management-plan.md) (see "Feature A").
>
> **Edit only this file for Feature A work** (avoids cross-feature merge conflicts).
> Build phases in order: build → test → merge → next. Don't start a phase until the
> previous is ✅. Status legend: ⬜ Not started · 🟡 In progress · ✅ Done · 🚧 Blocked.

**Owner:** Claude · **Status: 0 / 5 done — 🟡 In progress.**

| Phase | Scope                                                                       | Dev    | Status | Date       | Notes / PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----- | --------------------------------------------------------------------------- | ------ | ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A0    | Gym self-registration (`POST /gyms/register` + `app/register/` public form) | Claude | 🟡     | 2026-06-22 | In progress — contracts, API endpoint, web form built; auth gate fixed; suspend/reactivate with mandatory reason added; statusReason banner on detail page; reject/suspend send reason body; suspend+reactivate buttons added; auth layout guard with spinner; logout handles 401; Option B suspension flow: session kept alive, /auth/me returns gymStatus, useUser redirects to /suspended page which shows reason + logout button; word limits enforced via Zod refine (description ≤200 words, reject/suspend reason ≤80 words) with live counters on all three inputs; mandatory phone + address fields added to Gym model (migration 20260622151555); gym name uniqueness enforced via DB unique index (migration 20260622152000) with P2002 → 409 ConflictException in register() |
| A1    | Members admin CRUD (contracts + api + web)                                  | —      | ⬜     | —          | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| A2    | Plans admin catalog                                                         | —      | ⬜     | —          | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| A3    | Subscriptions (compute endDate, snapshot price, cancel)                     | —      | ⬜     | —          | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| A4    | Member invite + `MePortalModule` + `app/(member)/` shell + role redirect    | —      | ⬜     | —          | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

> A4 builds the shared member-portal shell that **B3** and **C3** depend on — once
> A4 is ✅, note it here so the other owners know they're unblocked.

## Decisions & deviations

- `Textarea` shadcn component was not pre-installed; added via `npx shadcn@latest add textarea` for the registration form description field.
- Registration sends a `SEND_GYM_PENDING` notification email (no magic link). The magic link is only created and sent when a SUPER_ADMIN calls `PATCH /gyms/:id/approve`. This enforces the approval gate end-to-end.
- `requestMagicLink` silently discards requests from ORG_ADMIN users whose gym is not `ACTIVE` (consistent with the non-existent-email pattern — no info leakage).
- `verifyMagicLink` throws `403 Forbidden` if the user is an ORG_ADMIN with a `PENDING`, `REJECTED`, or `SUSPENDED` gym — safety net for stale tokens.
- `PATCH /gyms/:id/reject` and `PATCH /gyms/:id/suspend` require a mandatory `reason` body field (Zod-validated); the reason is stored in `Gym.statusReason` and returned on `GET /gyms/:id`. Schema extended via migration `20260622133920_add_gym_status_reason`.
- `PATCH /gyms/:id/suspend` also immediately terminates all active sessions for the gym owner via `SessionService.deleteAllUserSessions`.
- `PATCH /gyms/:id/reactivate` sets status back to `ACTIVE`, clears `statusReason`, and sends the owner a fresh magic link.
- `description` on gym registration is limited to 200 words; `reason` on reject/suspend actions is limited to 80 words. Both limits are enforced via Zod `.refine()` in the shared contracts package (hits NestJS `ZodValidationPipe` server-side and `zodResolver` client-side). Frontend shows a live `X / N words` counter that turns red when the limit is exceeded and disables the submit/action button.
- `phone` (min 7, max 20 chars) and `address` are mandatory on gym registration — added to `Gym` schema, contracts, `GYM_DETAIL_SELECT`, and the registration form.
- Gym `name` is `@unique` in the DB. `register()` catches Prisma `P2002` and throws `ConflictException` (409) so the frontend `ApiError` surfaces a readable message.

## Notes for the next agent

- A0 is 🟡 in progress — end-to-end test needed: submit form → gym appears in SUPER_ADMIN `/gyms` list with status PENDING → SUPER_ADMIN approves → owner receives login-link email and can log in → confirm PENDING/REJECTED owners cannot log in. Confirm duplicate email rejected.
- Once A0 end-to-end test passes, mark A0 ✅ and move to A1.
