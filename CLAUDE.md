# CLAUDE.md

This repo uses `AGENTS.md` as the shared instruction file (it works across all AI coding tools the team uses).

**Read [`AGENTS.md`](AGENTS.md) first** — it contains all project conventions, the current build context (Gym Management System), and the pointer to the implementation plan.
