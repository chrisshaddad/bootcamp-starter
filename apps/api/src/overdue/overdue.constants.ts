export const OVERDUE_QUEUE = 'overdue';

export const OVERDUE_JOBS = {
  AGE_OVERDUE: 'age-overdue',
};

// How often the background sweep runs (hourly).
export const OVERDUE_SWEEP_INTERVAL_MS = 60 * 60 * 1000;
