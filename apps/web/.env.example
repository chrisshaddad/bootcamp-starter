# Browser requests stay same-origin. Next.js rewrites /api to API_ORIGIN.
NEXT_PUBLIC_API_URL=/api
# Retained for server-side routes that call the API directly.
API_URL=http://localhost:3001
API_ORIGIN=http://localhost:3001
