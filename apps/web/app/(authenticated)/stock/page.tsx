import { Package } from 'lucide-react';
import { PanelPlaceholder } from '@/components/panel-placeholder';

export default function StockPage() {
  return (
    <PanelPlaceholder
      icon={Package}
      title="Inventory"
      description="Track medicine batches, quantities, and expiry dates for your branch. This panel is being built."
    />
  );
}
