# Forward-Mena database. Local dev connects to the shared VPS Postgres via an
# SSH tunnel (see the repo infrastructure docs), e.g. 127.0.0.1:20009.
# Copy this file to .env.local and fill in real credentials.
DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:20009/prorentallb"
